export interface People {
    name: string;
    birthData: string;
    email: string;
    phone: string;
    password: string;
    summary: string;
}

export interface Character {
    id: number;
    name: string;
}

export interface Films {
    id: number;
    title: string;
}